[pytest]
testpaths = tests
addopts =
    -v
    --strict-markers
    --tb=short
    --color=no
    -p no:cacheprovider
timeout = 60
timeout_method = thread
markers =
    slow: marks tests that take more than ~5s to run
